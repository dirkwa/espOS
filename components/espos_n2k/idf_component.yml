## espos_n2k component manifest
dependencies:
  idf: ">=6.0.0"
  # mDNS left the IDF core in 5.0; exact-pinned like the other registry deps.
  espressif/mdns:
    version: "==1.11.3"
    rules:
      - if: "target != linux"
