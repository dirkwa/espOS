## espos_ble component manifest
dependencies:
  idf: ">=6.0.0"
  # The control channel is a WebSocket client; not part of IDF core.
  espressif/esp_websocket_client:
    version: "^1.5.0"
    rules:
      - if: "target != linux"
